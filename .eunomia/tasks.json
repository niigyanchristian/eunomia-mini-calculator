{
  "project_name": "eunomia-mini-calculator",
  "tasks": [
    {
      "task_id": "REQ-001",
      "task_type": "modify",
      "recommended_model": "haiku",
      "title": "Rename app title to \"ChriZ Cal\" in index.html, App.tsx, and App.test.tsx",
      "description": "1. WHAT: Update all title/heading text from the current values to \"ChriZ Cal\" to satisfy the PRD rename requirement.\n2. READ FIRST: Read `index.html` (line 7: <title>Chris's Calculator</title>), `src/App.tsx` (line 31: <h1>Mini Calculator</h1>), and `src/App.test.tsx` (multiple screen.getByText calls referencing \"Chris's Calculator\" on lines 14, 38, 52, 72, 92).\n3. FILES: Modify `index.html`, `src/App.tsx`, and `src/App.test.tsx`. No new files needed.\n4. APPROACH: Simple text replacement in three files — (a) in index.html change <title>Chris's Calculator</title> to <title>ChriZ Cal</title>, (b) in src/App.tsx change <h1>Mini Calculator</h1> to <h1>ChriZ Cal</h1>, (c) in src/App.test.tsx replace every occurrence of \"Chris's Calculator\" with \"ChriZ Cal\" (there are 5 occurrences on lines 14, 38, 52, 72, 92).\n5. PATTERNS: No structural changes — only string literals are updated.",
      "dependencies": [],
      "acceptance_criteria": [
        "The browser tab title reads \"ChriZ Cal\"",
        "The visible heading in the app UI reads \"ChriZ Cal\""
      ],
      "filename": "src/App.tsx",
      "language": "typescript"
    },
    {
      "task_id": "QA-001",
      "task_type": "qa_validation",
      "recommended_model": "haiku",
      "title": "Run full test suite to verify rename did not break anything",
      "description": "Run all existing tests in the project to verify they pass after the title rename in REQ-001. The updated test file is `src/App.test.tsx`. Also run the full suite to catch any regressions. Commands: `npx vitest run src/App.test.tsx` and `npx vitest run` for the full suite.",
      "dependencies": ["REQ-001"],
      "acceptance_criteria": [
        "npx vitest run src/App.test.tsx exits code 0 with all tests passing",
        "npx vitest run exits code 0 — all tests in the project pass",
        "npx tsc --noEmit exits code 0"
      ],
      "filename": "src/App.test.tsx",
      "language": "typescript"
    }
  ]
}
