{
  "project_name": "eunomia-mini-calculator",
  "tasks": [
    {
      "task_id": "QA-001",
      "task_type": "qa_validation",
      "recommended_model": "haiku",
      "title": "Run all tests and verify they pass",
      "description": "1. WHAT: Run the full test suite to verify all existing tests pass. 2. READ FIRST: src/App.test.tsx, src/components/Calculator.test.tsx, src/components/Display.test.tsx, src/components/Button.test.tsx — these are the existing test files. 3. FILES: No files to modify — validation only. 4. APPROACH: Run `npx vitest run` from the project root and confirm exit code 0 with all tests passing. 5. PATTERNS: Tests use Vitest with @testing-library/react; check the output for any failures or errors.",
      "dependencies": [],
      "acceptance_criteria": [
        "npx vitest run exits with code 0",
        "All test suites report 0 failures",
        "No TypeScript compilation errors: npx tsc --noEmit exits code 0"
      ],
      "filename": "src/components/Calculator.test.tsx",
      "language": "typescript"
    }
  ]
}
